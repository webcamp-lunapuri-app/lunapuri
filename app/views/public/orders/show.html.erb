<h2>注文履歴詳細</h2>
<h4>注文情報</h4>
<table>
  <tr><th>注文日</th><th><%= @order.created_at.strftime('%Y/%m/%d') %></th></tr>
  <tr><th>配送先</th><th><%= @order.delivery_postal_code + @order.delivery_address + @order.delivery_name %></th></tr>
  <tr><th>支払方法</th><th><%= @order.payment_methods %></th></tr>
  <tr><th>ステータス</th><th><%= @order.order_status %></th></tr>
</table>

<h4>請求情報</h4>
<table>
  <tr><th>商品合計</th><th><%= (@order.total_amounts - @order.delivery_charge).to_s(:delimited) %></th></tr>
  <tr><th>配送料</th><th><%= @order.delivery_charge.to_s(:delimited) %></th></tr>
  <tr><th>ご請求額</th><th><%= @order.total_amounts.to_s(:delimited) %></th></tr>
</table>

<h4>注文内容</h4>
<table>
  <tr>
    <td>商品名</td>
    <td>単価（税込）</td>
    <td>数量</td>
    <td>小計</td>
  </tr>
  <% @order.order_items.each do |order_item| %>
  <tr>
    <td><%= order_item.product.name %></td>
    <td><%= (order_item.purchase_price).to_s(:delimited) %></td>
    <td><%= order_item.count %></td>
    <td><%= (order_item.purchase_price * order_item.count).to_s(:delimited) %></td>
  </tr>
  <% end %>
</table>