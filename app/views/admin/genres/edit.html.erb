
<h2>ジャンル編集画面</h2>

<%= form_with model: @genre, url: admin_genre_path, local:true do |f| %>
  <h4>ジャンル名</h4>
  <%= f.text_field :name %>
  <h4>有効ステータス</h4>
  <%= f.check_box :is_active, {},"true","false" %><span>有効</span></br>
  <%= f.submit '変更を保存' %>
<% end %>

